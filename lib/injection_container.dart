import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

Future<void> init() async {
  // Navigation or other services could be registered here if needed.
  // For a static portfolio, we keep it lightweight.
}
